# Story.fun 「账号发 IP」改造 PRD

> 版本：v1.0
> 状态：待评审
> 范围：本次「账号发 IP」相关需求改造（最终需求形态）

---

## 一、背景与目标

### 1.1 现状问题

现有体系以「角色 IP」为核心：

```
用户发行角色 IP（NFT）→ 短剧绑定角色 IP → 用户签约角色 → 按角色绑定的短剧数据挖矿
```

流程四步、概念多重（发行/绑定/签约/挖矿），用户理解成本高。

### 1.2 改版目标

- 简化概念层级：去掉「角色 IP」中间层，账号即创作者即一个 IP
- 降低用户理解成本，兼顾 Web2 与 Web3 用户
- 保留创作者经济与收益分配闭环

### 1.3 改造范围

本 PRD 覆盖本次「账号发 IP」改造的最终需求：发行 IP、IP 市场、IP 收益、个人中心「发行的 IP」、发布/编辑短剧流程调整。

---

## 二、核心心智模型

```
账号 = 创作者 = 一个 IP
  ├── 名下所有短剧/短视频自动归属（无需手动绑定）
  └── 发行限量「IP 卡」
```

**一句话：账号即创作者 IP，发行限量 IP 卡，持卡按作品热度产出积分，按积分占比瓜分平台周奖池。**

---

## 三、功能需求

### A. 发行 IP

**入口**：顶部导航 / 底部导航 / 侧边栏「发行IP」。

**流程**：

| 步骤 | 内容 |
|---|---|
| 1. IP 形象 | 使用账号头像（个人中心可更换） |
| 2. IP 名称/简介 | 默认带出账号用户名与简介，可修改（名称 ≤20 字，简介 ≤500 字） |
| 3. 发行参数 | 发行总量 100–5000（选完锁定，永不扩容）；定价模式：固定价格 / 曲线价格；价格（USDC） |
| 4. 手续费 | 1 USDC |
| 5. 发行成功 | 展示 IP 名称 + 23 位 IP 编号；主按钮「查看 IP 合集」进入 IP 详情页 |

**规则**：
- 发行者同样需购买 IP 卡方可持有（发行不自动持有）
- 每个账号当前发行 1 个 IP（多 IP 支持后续迭代）

### B. IP 市场

**入口**：「IP 市场」。

**内容**：平台 IP 卡合集列表，卡片展示：

- IP 形象（头像）
- IP 名称
- 发行价（USDC）
- 发行量（共 N 张）
- IP 编号（23 位数字，完整显示）

**交互**：点击卡片进入 IP 详情页；点击「购买」弹出购买确认（价格、数量、购买价语义），购买成功即持有。

### C. IP 收益（原「经纪人」页）

**玩法**（对象为 IP 卡）：

| 状态 | 说明 |
|---|---|
| 激活中 | IP 卡激活 → 产出积分；激活期间消耗能量 |
| 未激活 | IP 卡未激活 → 不产出积分 |
| 停用 | 用户主动停止产出，停用期间恢复能量 |
| 能量 | 激活期间消耗，耗尽自动转未激活；可补充 |

**积分产出**：

```
积分 = 价格系数 × 热度系数
```

- 价格系数：由发行初始价格 P0 决定（P0 ≤ 100 USDC 线性；>100 增速渐缓，上限 1.6）
- 热度系数：IP 参演短剧的完播、点赞、收藏、评分等互动数据（**短剧热度驱动**）
- 单位：积分/h

**收益结算**：按积分占比瓜分平台每周 STORY 奖池。

**升级**：消耗 1 张同 IP 同等级卡 + 经验包（原训练手册），主体卡升级（Lv1→Lv2…Lv5），总量通缩、主卡保留。

### D. 个人中心

- **「发行的 IP」区块**：展示账号已发行的 IP 卡（头像、名称、发行价、发行量、23 位编号）；未发行显示空态引导「将账号发行成 IP，获得购买 IP 卡的分成 · 持卡赚取 STORY 奖励」+ 去发行；点击卡片进入 IP 详情
- **他人主页**：展示该用户「TA 发行的 IP」（仅展示）
- **统计项**：关注 / 粉丝 / 互关 / 获赞

### E. 发布 / 编辑短剧

- **两步流程**：基本信息 → 剧集管理（不再有「绑定 IP」步骤）
- 短剧/短视频自动归属发布者账号（账号 = 创作者 = IP），无需手动绑定

---

## 四、关键规则与数据

### 4.1 IP 编号

- 23 位数字格式（首位非 0），如 `31389183018301831830801`
- 发行时生成，唯一、不可修改

### 4.2 卡片升级（2 合 1）

```
1 张主体卡（保留，升级）+ 1 张同等级耗材卡（销毁）+ 经验包
→ 主体卡升 1 级
```

- 耗材销毁 → 总量通缩，价值保留
- 主体保留 → 用户不损失主卡

### 4.3 收益来源

| 来源 | 说明 |
|---|---|
| 卖卡收入分成 | USDC |
| 持卡积分瓜分奖池 | STORY（按积分占比瓜分平台周奖池） |
| 交易版税 | USDC（二级市场，后续迭代） |

---

## 五、词汇表

| 旧词 | 新词 | 说明 |
|---|---|---|
| 角色 IP / 角色 | 创作者 IP | 账号 = 创作者 |
| 签约角色 / 签约 | 购买 IP 卡 / 购买 | 购买 NFT 获得 IP 卡 |
| NFT | IP 卡 | Web2 友好，限量、可升级、可转售 |
| 片酬 / 片酬/h | 积分 / 积分/h | 产出单位 |
| 角色 IP 市场页 | IP 市场 | 购买 IP 的市场 |
| 经纪人页 | IP 收益 | 持卡 + 积分 + 收益 |
| 演出 | 激活 | IP 卡激活中 → 产出积分 |
| 候场 | 未激活 | IP 卡未激活 → 不产出积分 |
| 休息 | 停用 | 用户主动停止产出 |
| 体力 | 能量 | 激活期间消耗，耗尽自动转未激活 |
| 体力包 | 能量包 | 恢复卡片产出 |
| 训练手册 | 经验包 | 提升收益权重 |
| 挖矿 | 瓜分奖池 | 积分占比瓜分平台周奖池 |

---

## 六、非目标（Out of Scope）

- 二级市场转售完整交互（当前仅文案提及，后续迭代）
- 共同创作（共创）流程（后续迭代）
- 其他未在「三、功能需求」中列出的功能

---

## 七、验收标准（摘要）

- 全站无「角色 IP」「签约」「NFT」「片酬」「演出/候场/休息/体力」「训练手册」「挖矿」「经纪人」等旧词残留（用户可见文案）
- 发行 IP → IP 市场购买 → IP 收益激活 → 积分瓜分奖池，链路可走通
- 发布/编辑短剧为两步流程，无「绑定 IP」
- 个人中心/他人主页展示「发行的 IP」，统计为关注/粉丝/互关/获赞
